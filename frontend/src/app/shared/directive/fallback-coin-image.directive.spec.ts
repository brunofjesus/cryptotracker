import { FallbackCoinImageDirective } from './fallback-coin-image.directive';

describe('FallbackCoinImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackCoinImageDirective();
    expect(directive).toBeTruthy();
  });
});
